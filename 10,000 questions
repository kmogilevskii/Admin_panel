Чтобы при создании записи в промежуточной таблице джанго генерировал uuid, я выставил значение в моделях default=uuid. Почему django при удалении записи в промежуточной таблице(GenreFilmWork или PersonFilmWork) теперь генерирует uuid(то есть он отправляет в бд запрос на удаление записи, id которой он только что сам придумал => бд удаляет несуществующую запись и отправляет ответ, что удаление успешно и эти два болвана счастливы), и правильно ли я сделал, установив параметр default = uuid?(меня этот вопрос беспокоит ещё с первого спринта если честно) 

Как сделать, чтобы по нажатию на sh файл он сразу открывался в терминале и запрашивал пароль? И можно ли так сделать? (и нужно ли?)

Всё равно не до конца понимаю, куда запихнуть secretkey django. Я конечно могу у себя на компьютере прописать его переменной окружения на время разработки, но если я его использую в контейнере, мне же всё равно так или иначе придётся явно его прописать в докерфайле, и он всё равно будет hardcoded into file, или нет?

Если у нас есть отдельные requiremnts.txt для дева и прода джанги, это значит, что у нас должны быть отдельные докерфайлы, а следовательно разные образы и контейнеры для них? Dockerfile stages? Как настроить его в compose?

Стоит ли сделать общий volume между джангой и энджинксом в который при помощи команды «sudo docker exec -it admin_panel_django_1 python3 manage.py collectstatic» будут переливаться статик файлы?

И я сдаюсь, как выполнить третий пункт nginx (про админку без статики)?
